import React, { useEffect, useState, useContext } from "react"

const NotFoundPage = ({ }) => {
    useEffect(() => {

    }, [])
    return (
        <>

        </>
    )
};

export default NotFoundPage;
